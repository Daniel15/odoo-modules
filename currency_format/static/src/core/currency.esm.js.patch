--- a/addons/web/static/src/core/currency.js
+++ b/currency_format/static/src/core/currency.esm.js
@@ -0,0 +1,3 @@
+/* @odoo-module alias=@web/core/currency default=false */
+/* eslint-disable capitalized-comments, init-declarations, jsdoc/check-types, no-negated-condition, sort-imports */
+
@@ -21,0 +25 @@
+ * @param {Object} [options.currency] currency metadata to use instead of currencyId
@@ -33 +37,2 @@
-    const currency = getCurrency(currencyId);
+    // currency_format: Currency previews can supply unsaved currency metadata.
+    const currency = options.currency || getCurrency(currencyId);
@@ -51 +56,2 @@
-    return formatted.join(nbsp);
+    // currency_format: Core always joins these parts with a non-breaking space.
+    return formatted.join(currency.symbol_spacing === "no_space" ? "" : nbsp);
