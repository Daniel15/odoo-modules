This module makes the spacing between a currency symbol and its amount
configurable per currency. It combines symbol position and spacing into one
Display Format field with four choices: `$123.45`, `$ 123.45`, `123.45$`, and
`123.45 $`.

On installation, the spacing for existing curencies will be set based on its
default configuration in CLDR.
