The module sets useful defaults, but you can change the settings for any currency.
Enable developer mode, go to **Settings → Currencies**, click a currency, then
select the desired **Display Format**.
