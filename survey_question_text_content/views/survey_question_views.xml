<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2026 Daniel Lo Nigro
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl.html). -->
<odoo>
    <record id="survey_question_form_text_content" model="ir.ui.view">
        <field name="name">survey.question.form.text_content</field>
        <field name="model">survey.question</field>
        <field name="inherit_id" ref="survey.survey_question_form" />
        <field name="arch" type="xml">
            <!-- Add preview for text_content question type -->
            <xpath
                expr="//div[@invisible=&quot;question_type != 'scale'&quot;]"
                position="after"
            >
                <div invisible="question_type != 'text_content'">
                    <i
                        class="fa fa-align-justify fa-4x"
                        role="img"
                        aria-label="Text content"
                        title="Text Content"
                    />
                </div>
            </xpath>

            <!-- Add content_html editor inside the Answers tab -->
            <xpath
                expr="//page[@name='answers']/field[@name='matrix_row_ids']"
                position="after"
            >
                <field
                    name="content_html"
                    widget="html"
                    options="{'embedded_components': false}"
                    placeholder="Enter the text content to display in the survey..."
                    invisible="question_type != 'text_content'"
                />
            </xpath>

            <!-- Hide the Description tab for text_content -->
            <xpath expr="//page[@name='survey_description']" position="attributes">
                <attribute name="invisible">question_type == 'text_content'</attribute>
            </xpath>

            <!-- Hide non-relevant option groups for text_content -->
            <xpath
                expr="//page[@name='options']//group[field[@name='validation_required']]"
                position="attributes"
            >
                <attribute
                    name="invisible"
                >question_type in ['scale', 'text_content']</attribute>
            </xpath>
            <xpath
                expr="//page[@name='options']//group[field[@name='constr_mandatory']]"
                position="attributes"
            >
                <attribute name="invisible">question_type == 'text_content'</attribute>
            </xpath>
            <xpath
                expr="//page[@name='options']//group[field[@name='session_available']]"
                position="attributes"
            >
                <attribute name="invisible">question_type == 'text_content'</attribute>
            </xpath>
        </field>
    </record>
</odoo>
